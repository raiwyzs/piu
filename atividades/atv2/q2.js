// Declara uma variável 'n2' e atribui o valor 7 (a tabuada a ser exibida)
let n2 = 7;

// Loop for que vai de 1 até 10 (inclusive) para mostrar a tabuada
for (let i = 1; i <= 10; i++) {
  // Em cada iteração, imprime no console a multiplicação formatada
  console.log(n2 + " x " + i + " = " + (n2 * i));
}